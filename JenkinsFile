pipeline {
    agent any

    tools{
        maven 'Maven'
    }

    stages {
        stage("Checkout") {
            steps {
                git branch: 'main', url: 'https://github.com/AdityaS358/task6.git'
            }
        }

        stage("Build") {
            steps {
                mvn clean
            }
        }

        stage("Test") {
            steps {
                mvn test
            }
        }

        stage("Deploy") {
            steps {
                echo 'Deployment Stage Successful!'
            }
        }
    }
}
